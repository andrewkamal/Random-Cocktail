<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cocktail Time!</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Satisfy&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="styles/main.css">
</head>
<body>
    <div class="px-4 py-2 text-center">
        <% if(!locals.drink) { %>
        <h1>Welcome to Random Cocktail!</h1>
        <p>Click the button below to get a random cocktail recipe!</p>
        <% } else { %>
        <h1>Random Cocktail</h1>
        <h2 class="my-4"><%= locals.drink.strDrink %></h2>
        <img class="mt-4" src="<%= locals.drink.strDrinkThumb %>" alt="<%= locals.drink.strDrink %>">
    </div>
    
    <div class="px-4 py-1 text-center">
        <h2>Type</h2>
        <p class="lead mb-2"><%= locals.drink.strAlcoholic %></p>
    </div>

    <div class="px-4 py-1 text-center">
        <h2 class="Ing">Ingredients</h2>
        <ul>
            <div class="text-center justify-content-sm-center col-md">
            <% for(let i = 1; i <= 15; i++) { %>
                <% if(locals.drink[`strIngredient${i}`]) { %>
                    <% if(locals.drink[`strMeasure${i}`]) { %>
                        <li><%= locals.drink[`strMeasure${i}`] + " of " + locals.drink[`strIngredient${i}`] %></li>
                    <% } else { %>
                        <li><%= locals.drink[`strIngredient${i}`] %></li>
                <% } %>
                <% } %>
            <% } %>
            </div>
        </ul>
    </div>

    <div class="px-4 py-1 text-center">
        <h2>Instructions</h2>
        <p><%= locals.drink.strInstructions %></p>
        <% } %>  
    </div>

    <div class="d-grid gap-1 d-sm-flex justify-content-sm-center">
        <form action="/random" method="GET">
        <button type="submit" class="update-Button btn btn-lg px-4 gap-3 my-4">Random Cocktail</button>
        </form>
    </div>

    <%- include('partials/footer') %>
</body>
</html>